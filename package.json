{
  "name": "job-seeker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "concurrently -n api,vue -c cyan,green \"nodemon --ignore src/ --ignore public/ --ignore dist/ server.js\" \"vite\"",
    "build": "vite build",
    "start": "NODE_ENV=production node --no-warnings server.js"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "vue": "^3.4.0"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^5.0.0",
    "concurrently": "^8.2.2",
    "nodemon": "^3.1.0",
    "vite": "^5.0.0"
  }
}
